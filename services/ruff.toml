# Shared ruff settings for the five services. The rules are listed on purpose
# instead of taking ruff's defaults, so a new ruff version can't change what
# the CI checks without us noticing. To add rules, add them here.
line-length = 120
target-version = "py312"

[lint]
select = ["E4", "E7", "E9", "F"]
